function w_cmd = rotate(speed, angle, clockwise)

if clockwise:
    w_cmd = -abs(speed);
else:
    w_cmd = abs(speed);
end
%myFun - Description
%
% Syntax: output = myFun(input)
%
% Long description
    
end